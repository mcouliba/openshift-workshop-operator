apiVersion: openshift.redhat.com/v1alpha1
kind: Workshop
metadata:
  name: debugging-workshop
spec:
  user: 
    number: 10
    password: openshift
  source:
    gitURL: https://github.com/mcouliba/debugging-workshop
    gitBranch: "3.1"
  infrastructure:
    argocd:
      enabled: true
    codeReadyWorkspace:
      enabled: true
      operatorHub:
        channel: latest
        clusterServiceVersion: crwoperator.v2.0.0
    etherpad:
      enabled: true
    gogs:
      enabled: false
      image:
        name: quay.io/gpte-devops-automation/gogs-operator
        tag: v0.12
    nexus:
      enabled: true
    pipeline:
      enabled: false
      operatorHub:
        channel: dev-preview
        clusterServiceVersion: openshift-pipelines-operator.v0.8.2
    project:
      enabled: true
      devName: dev-project
      stagingName: staging-project
    serviceMesh:
      enabled: true
      serviceMeshOperatorHub:
        channel: "1.0"
        clusterServiceVersion: servicemeshoperator.v1.0.7
    istioWorkspace:
      enabled: false
    workshopper:
      enabled: true